#!/usr/bin/env python3
"""Punkt wejścia `sf-kit`. Wyłącznie biblioteka standardowa — żadnego `pip install`."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from sf_kit.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
